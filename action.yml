name: 'Test Action'
description: 'Action for testing'
inputs:
  message:
    description: 'Message from container'
    required: true
    default: 'I'm alive'
outputs:
  time: # id of output
    description: 'The time we greeted you'
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.message }}